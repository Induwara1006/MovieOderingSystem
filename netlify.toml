# Netlify configuration for React app
[build]
  command = "npm run build"
  publish = "build"

# Redirect all requests to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
